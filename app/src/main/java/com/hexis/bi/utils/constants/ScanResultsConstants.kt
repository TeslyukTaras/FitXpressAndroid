package com.hexis.bi.utils.constants

internal object ScanResultsConstants {

    /**
     * Camera distance multiplier for the legacy Scan Results screen avatar previews.
     * >1 pulls the camera further from the body so the figure sits smaller in the frame.
     */
    const val MODEL_DISTANCE_SCALE = 1.3f
}
