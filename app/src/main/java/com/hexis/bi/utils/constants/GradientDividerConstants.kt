package com.hexis.bi.utils.constants

internal object GradientDividerConstants {

    const val EDGE_STOP = 0f
    const val CENTER_STOP = 0.5f
    const val END_STOP = 1f

    /** For edge-only fades: the line stays solid except this fraction of the length at each end. */
    const val EDGE_FADE_FRACTION = 0.025f
}
