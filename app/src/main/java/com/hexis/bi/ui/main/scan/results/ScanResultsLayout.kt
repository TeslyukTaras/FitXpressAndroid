package com.hexis.bi.ui.main.scan.results

/**
 * Row/column [androidx.compose.foundation.layout.RowScope] / [ColumnScope] weight values for Scan Results.
 */
internal object ScanResultsLayout {
    /** Fallback scale when transform width/height is invalid (maps overlay 1:1 to layout). */
    const val OVERLAY_SCALE_FALLBACK = 1f

    /** Vertical fraction along pill height where leader line attaches (midline). */
    const val PILL_LEADER_ATTACH_Y_FRACTION = 0.5f
}
