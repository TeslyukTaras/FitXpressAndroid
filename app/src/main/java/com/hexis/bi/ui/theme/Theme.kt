package com.hexis.bi.ui.theme

import androidx.compose.material3.ColorScheme
import androidx.compose.ui.graphics.Color

/**
 * Subtle edge for overlay surfaces (dialogs, bottom sheets): a contrasting `#FFFFFF66` hairline
 * that reads against the app's dark surfaces.
 */
fun ColorScheme.overlayBorder(): Color = OverlayBorder
